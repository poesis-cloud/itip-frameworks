{
  "$schema": "gsmarc://gsm/Archetype/v1",
  "$id": "gsmarc://itip/frameworks/iso25000/iso25012/inherent-and-system-quality/DataPrecision/v1",
  "title": "DataPrecision",
  "description": "ISO 25012-sourced data quality archetype — Precision inherent-and-system data quality characteristic. The degree to which data has attributes that are exact or that provide discrimination in a specific context of use. Rootless archetype — used as qualifier in Directives/Norms governing data entities. Source: ISO/IEC 25012 (Inherent and System-Dependent Data Quality — Precision).",
  "type": "object",
  "required": [
    "precisionLevel"
  ],
  "properties": {
    "precisionLevel": {
      "type": "string",
      "enum": [
        "LOW",
        "MODERATE",
        "HIGH",
        "MAXIMUM"
      ],
      "$gsm:queryable": true,
      "description": "Required level of data precision/granularity. LOW = coarse values sufficient. MODERATE = standard precision. HIGH = fine-grained detail required. MAXIMUM = highest available precision."
    },
    "decimalPlaces": {
      "type": "integer",
      "minimum": 0,
      "description": "Required number of decimal places for numeric data."
    },
    "significantDigits": {
      "type": "integer",
      "minimum": 1,
      "description": "Required number of significant digits for numeric data."
    }
  },
  "additionalProperties": false
}
