{
  "$schema": "gsmarc://gsm/Archetype/v1",
  "$id": "gsmarc://itip/frameworks/iso25000/iso25010/product-quality/ProductAvailability/v1",
  "title": "ProductAvailability",
  "framework": {
    "name": "iso25000",
    "version": "1.0.0"
  },
  "description": "TOGAF-sourced quality archetype \u2014 Availability viability dimension. Defines the property vocabulary for availability governance: uptime targets, recovery objectives, redundancy. Rootless archetype (no structural allOf) \u2014 used as qualifier in Directives/Norms and as data type for Effectors/Receptors carrying availability state. Norm predicates reference properties by Availability.{property} in CEL expressions. Source framework: TOGAF (Architecture Principles, Non-Functional Requirements) + ISO/IEC 25010 (Availability sub-characteristic).",
  "type": "object",
  "required": [
    "uptimePercent"
  ],
  "properties": {
    "uptimePercent": {
      "type": "number",
      "minimum": 0,
      "maximum": 100,
      "$gsm:queryable": true,
      "description": "Service availability as a percentage (e.g., 99.99). Norm example: Availability.uptimePercent >= 99.9."
    },
    "recoveryTimeObjective": {
      "type": "string",
      "pattern": "^P",
      "description": "Maximum acceptable duration to restore service after failure (ISO 8601 duration, e.g., 'PT15M' for 15 minutes)."
    },
    "recoveryPointObjective": {
      "type": "string",
      "pattern": "^P",
      "description": "Maximum acceptable data loss measured in time (ISO 8601 duration, e.g., 'PT1H' for 1 hour of data loss tolerance)."
    },
    "maintenanceWindowDuration": {
      "type": "string",
      "pattern": "^P",
      "description": "Planned maintenance window duration (ISO 8601 duration, e.g., 'PT4H')."
    },
    "redundancyMode": {
      "type": "string",
      "enum": [
        "NONE",
        "ACTIVE_PASSIVE",
        "ACTIVE_ACTIVE",
        "MULTI_REGION"
      ],
      "$gsm:queryable": true,
      "description": "Infrastructure redundancy topology. NONE = single instance. ACTIVE_PASSIVE = standby failover. ACTIVE_ACTIVE = load-balanced replicas. MULTI_REGION = geo-distributed."
    }
  },
  "additionalProperties": false
}