{
  "$schema": "gsmarc://gsm/Archetype/v1",
  "$id": "gsmarc://itip/frameworks/togaf/governance/Driver/v1",
  "title": "Driver",
  "description": "TOGAF-sourced archetype — an external or internal condition that motivates the organization to define Goals and shape architecture direction. Drivers are the root cause of change and the origin of the motivation chain (Driver → Goal → Objective → Requirement). Rootless — a Driver is a classification of motivation, not an active system. TOGAF mapping: Driver (Motivation Extension, Architecture Vision). Source framework: TOGAF (Motivation Extension, Architecture Vision).",
  "type": "object",
  "required": [
    "driverType"
  ],
  "properties": {
    "driverType": {
      "type": "string",
      "enum": [
        "BUSINESS",
        "TECHNOLOGY",
        "REGULATORY",
        "MARKET",
        "ORGANIZATIONAL"
      ],
      "$gsm:queryable": true,
      "description": "Driver classification by origin. BUSINESS = internal business need. TECHNOLOGY = technology change or opportunity. REGULATORY = compliance/legal requirement. MARKET = competitive/market pressure. ORGANIZATIONAL = internal organizational change. TOGAF mapping: Driver categorization in Architecture Vision."
    },
    "source": {
      "type": "string",
      "enum": [
        "INTERNAL",
        "EXTERNAL"
      ],
      "$gsm:queryable": true,
      "description": "Whether the driver originates from within or outside the enterprise boundary."
    },
    "urgency": {
      "type": "string",
      "enum": [
        "IMMEDIATE",
        "SHORT_TERM",
        "MEDIUM_TERM",
        "LONG_TERM"
      ],
      "$gsm:queryable": true,
      "description": "Urgency of the driver. TOGAF mapping: prioritization input for Architecture Vision."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable description of the driver."
    }
  },
  "additionalProperties": false
}
