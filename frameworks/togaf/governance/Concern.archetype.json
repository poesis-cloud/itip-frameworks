{
  "$schema": "gsmarc://gsm/Archetype/v1",
  "$id": "gsmarc://itip/frameworks/togaf/governance/Concern/v1",
  "title": "Concern",
  "framework": {
    "name": "togaf",
    "version": "1.0.0"
  },
  "description": "TOGAF-sourced justification archetype \u2014 Architecture Concern. Defines the property vocabulary for capturing stakeholder concerns that drive architecture decisions and governance priorities. Rootless archetype \u2014 used to type concern records in governance justification. TOGAF mapping: Concern (Stakeholder Management, Architecture Requirements specification \u2014 the interests that stakeholders have in the architecture). Source framework: TOGAF (Stakeholder Management, Architecture Governance).",
  "type": "object",
  "required": [
    "category"
  ],
  "properties": {
    "category": {
      "type": "string",
      "enum": [
        "RISK",
        "REQUIREMENT",
        "CONSTRAINT",
        "ASSUMPTION",
        "DEPENDENCY"
      ],
      "$gsm:queryable": true,
      "description": "Concern classification. RISK = potential negative outcome. REQUIREMENT = must-have capability. CONSTRAINT = non-negotiable boundary. ASSUMPTION = unverified premise. DEPENDENCY = external factor. TOGAF mapping: Concern categorization in Architecture Requirements Specification."
    },
    "priority": {
      "type": "string",
      "enum": [
        "CRITICAL",
        "HIGH",
        "MEDIUM",
        "LOW"
      ],
      "$gsm:queryable": true,
      "description": "Priority level reflecting impact on architecture decisions. Drives Architecture Requirement prioritization."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable description of the concern."
    }
  },
  "additionalProperties": false
}