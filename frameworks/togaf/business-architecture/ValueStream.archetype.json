{
  "$schema": "gsmarc://gsm/Archetype/v1",
  "$id": "gsmarc://itip/frameworks/togaf/business-architecture/ValueStream/v1",
  "title": "ValueStream",
  "description": "TOGAF-sourced representation archetype — an end-to-end representation of value delivery from trigger to stakeholder outcome, spanning multiple GSM Structures. A cross-structural representation of how value flows through organizational capabilities. Rootless, domain-scoped. TOGAF mapping: Value Stream (Business Architecture, Value Stream Mapping). Source framework: TOGAF (Business Architecture, Value Stream Mapping).",
  "type": "object",
  "required": [
    "valueType"
  ],
  "properties": {
    "valueType": {
      "type": "string",
      "enum": [
        "REVENUE_GENERATING",
        "COST_REDUCING",
        "ENABLING"
      ],
      "$gsm:queryable": true,
      "description": "Value stream contribution type. REVENUE_GENERATING = directly produces revenue. COST_REDUCING = reduces operational costs. ENABLING = supports other value streams. TOGAF mapping: Value Stream value classification."
    },
    "stageCount": {
      "type": "integer",
      "minimum": 1,
      "$gsm:queryable": true,
      "description": "Number of value-adding stages in the stream. TOGAF mapping: Value Stream stage count."
    }
  },
  "additionalProperties": false
}
