{
  "$schema": "gsmarc://gsm/Archetype/v1",
  "$id": "gsmarc://itip/frameworks/togaf/business-architecture/BusinessEvent/v1",
  "title": "BusinessEvent",
  "framework": {
    "name": "togaf",
    "version": "1.0.0"
  },
  "description": "TOGAF-sourced archetype \u2014 event classification for data flowing through Interactions. A business event signals that something of governance relevance has occurred. Used as the data Archetype for Effectors and Receptors (archetype field). Rootless \u2014 not a Structure, Mechanism, or Interaction; it types the payload exchanged between them. Source framework: TOGAF (Content Metamodel \u2192 Business Event).",
  "type": "object",
  "required": [
    "eventType"
  ],
  "properties": {
    "eventType": {
      "type": "string",
      "enum": [
        "DOMAIN",
        "INTEGRATION",
        "LIFECYCLE",
        "COMMAND",
        "NOTIFICATION"
      ],
      "$gsm:queryable": true,
      "description": "Event classification. DOMAIN = business state change, INTEGRATION = system boundary crossing, LIFECYCLE = entity lifecycle transition, COMMAND = action request, NOTIFICATION = informational broadcast. TOGAF mapping: Business Event type."
    },
    "triggerMode": {
      "type": "string",
      "enum": [
        "AUTOMATIC",
        "MANUAL",
        "SCHEDULED"
      ],
      "$gsm:queryable": true,
      "description": "How the event is triggered. TOGAF mapping: Event trigger mechanism."
    }
  },
  "additionalProperties": false
}