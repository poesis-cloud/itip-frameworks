{
  "$schema": "gsmarc://gsm/Archetype/v1",
  "$id": "gsmarc://itip/frameworks/togaf/adm/TransitionArchitecture/v1",
  "title": "TransitionArchitecture",
  "framework": {
    "name": "togaf",
    "version": "1.0.0"
  },
  "description": "TOGAF-sourced archetype \u2014 a formally defined intermediate architecture state between the Baseline Architecture and the Target Architecture. Represents a planned waypoint in the architecture evolution roadmap. Placed in unclassified/ for GSM placement deliberation: may be a directive (target state specification), a governance record, or a state-plane snapshot definition. TOGAF mapping: Transition Architecture (ADM Phase E/F, Migration Planning). Source framework: TOGAF (ADM, Migration Planning).",
  "type": "object",
  "required": [
    "sequenceOrder"
  ],
  "properties": {
    "sequenceOrder": {
      "type": "integer",
      "minimum": 1,
      "$gsm:queryable": true,
      "description": "Position in the transition sequence (1 = first transition from baseline). TOGAF mapping: Transition Architecture ordering in migration roadmap."
    },
    "targetState": {
      "type": "string",
      "enum": [
        "BASELINE",
        "TRANSITION",
        "TARGET"
      ],
      "$gsm:queryable": true,
      "description": "Architecture state classification. BASELINE = current as-is. TRANSITION = intermediate planned state. TARGET = final to-be. TOGAF mapping: Architecture state in roadmap."
    },
    "plannedDuration": {
      "type": "string",
      "pattern": "^P",
      "description": "Planned duration for this transition as ISO 8601 duration (e.g., 'P6M' for six months). TOGAF mapping: Migration Planning timeline."
    }
  },
  "additionalProperties": false
}