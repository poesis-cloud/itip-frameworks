{
  "$schema": "gsmarc://gsm/Archetype/v1",
  "$id": "gsmarc://itip/frameworks/togaf/data-architecture/PhysicalDataComponent/v1",
  "$ref": "gsmarc://gsm/Structure/v1",
  "title": "PhysicalDataComponent",
  "description": "TOGAF-sourced structural archetype \u2014 physical data storage and access infrastructure (database, file system, data lake). An active system: it has Mechanisms (CRUD operations, query processing, replication), Effectors (query results, change events), Receptors (writes, commands). Extends Structure with TOGAF Data Architecture vocabulary. TOGAF mapping: Physical Data Component (Content Metamodel, Data Architecture domain). Source framework: TOGAF (Data Architecture, Content Framework).",
  "type": "object",
  "required": [
    "storageType"
  ],
  "properties": {
    "storageType": {
      "type": "string",
      "enum": [
        "RELATIONAL_DATABASE",
        "DOCUMENT_STORE",
        "GRAPH_DATABASE",
        "FILE_SYSTEM",
        "OBJECT_STORE",
        "DATA_LAKE"
      ],
      "$gsm:queryable": true,
      "description": "Storage technology classification. TOGAF mapping: Physical Data Component storage mechanism type."
    },
    "persistenceModel": {
      "type": "string",
      "enum": [
        "PERSISTENT",
        "EPHEMERAL",
        "CACHED"
      ],
      "$gsm:queryable": true,
      "description": "Data persistence characteristic. TOGAF mapping: informs data lifecycle and governance strategy."
    }
  },
  "unevaluatedProperties": false
}
